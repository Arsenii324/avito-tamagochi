# Использование ИИ

Кейс требует указать, на каких этапах и как использовались нейросети.

**Это не ежедневный журнал.** Мы пробовали формулировку «ведём с первого дня» и честно
признали, что четыре человека под дедлайн его вести не будут. Поэтому источник — история
git, а этот файл собирается один раз, в день 6, перед защитой.

Что делает историю пригодной для восстановления (соблюдаем по ходу, а не в конце):

- Conventional Commits и осмысленные описания PR.
- Коммиты, сделанные с помощью ИИ, несут `Co-Authored-By` соответствующего инструмента.
- Если ИИ повлиял на решение, а не только на код, — строка «почему» в
  `docs/DECISIONS.md` пишется человеком и своими словами.

Сборка черновика в день 6:

```bash
git log --pretty='%ad %an %s' --date=short --no-merges | sort
git log --grep='Co-Authored-By' --pretty='%ad %an %s' --date=short
```

## Черновик таблицы (дополняется в день 6)

Строки пишет участник о себе — за другого не заполняем. Ниже — только та строка,
которую может написать сама сессия ассистента (09.08); остальные даты в
`git log --pretty='%ad %an %s' --date=short --no-merges | sort` принадлежат другим
участникам и ждут, когда каждый заполнит свою.

| Дата | Участник | Этап / часть | Как использовался |
|---|---|---|---|
| 06.08 | _(заполнить)_ | Процесс: `AGENTS.md`, `README.md`, `docs/*`, `.golangci.yaml`, CI, `.claude/*` | _(заполнить: что именно делал ассистент, что решал человек)_ |
| 09.08 | Claude Code (сессия под аккаунтом Arseniy Varaksin) | `internal/pet` (распад, уход, XP, уровни, WS-пуш), `internal/social` (лидерборд, сводка дня), `backend/cmd/seed` и `/debug/clock` (демо-стенд), минимальный `frontend/` поверх готового бэкенда | Ассистент писал код, тесты и миграции самостоятельно в рамках уже принятых `AGENTS.md`/`docs/DECISIONS.md`; решения, которые эти файлы явно относят к «стоп-вопросам» (entitlement наград, кривая уровней, концепт питомца), не принимались и не обходились. Пять реальных багов найдено и исправлено по ходу (округление распада, дедлайн WS-сокета через доменные часы, регистр JSON-полей, мёртвая ветка `ParseScope`, отсутствие `binding:"required"` у сгенерированного тела `POST /summary/daily/seen`) — подробности в сообщениях коммитов и PR |

## В самом продукте

Кейс требует LLM-интеграцию в сам продукт (`aiNote` — персональная фраза от питомца в
сводке дня, `docs/openapi.json` → `DailySummary.aiNote`). **Это не сделано.** Сводка дня
(`GET /summary/daily`) построена и покрыта тестами, но `aiNote` в ответе отсутствует
(`omitempty`) — честное отсутствие, не заглушка со случайным текстом. AGENTS.md отмечает
её отдельно как «case-optional bonus», а не обязательную часть уже принятых решений, но
для пункта кейса «интеграция ИИ в MVP» это означает: на сегодня в самом продукте ИИ не
используется вообще, только в разработке (см. таблицу выше).
